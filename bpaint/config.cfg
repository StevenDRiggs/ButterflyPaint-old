import os

curr_dir = os.getcwd()
db_dir = os.path.join(curr_dir, 'bpaint', 'db')

try:
  os.scandir(db_dir)
except FileNotFoundError:
  os.mkdir(db_dir)

SQLALCHEMY_DATABASE_URI = f'sqlite:///{db_dir}/bpaint.sqlite'
SQLALCHEMY_TRACK_MODIFICATIONS = False
